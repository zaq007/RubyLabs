<%= form_for(trip) do |f| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_in %>
    <%= f.datetime_select :time_in %>
  </div>

  <div class="field">
    <%= f.label :time_out %>
    <%= f.datetime_select :time_out %>
  </div>

  <div class="field">
    <%= f.label :plane_id %>
    <%= f.text_field :plane_id %>
  </div>

  <div class="field">
    <%= f.label :airport_from %>
    <%= f.text_field :airport_from_id %>
  </div>

  <div class="field">
    <%= f.label :airport_to %>
    <%= f.text_field :airport_to_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
